services:
  - type: web
    name: epmi-gaming
    env: node
    plan: free
    buildCommand: node scripts/verify-dist.js
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
